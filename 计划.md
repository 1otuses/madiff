# Per-Agent Credit 条件化 MADiff 研究计划

## Material Passport

- Origin Skill: `academic-research-suite / experiment-agent`
- Origin Mode: `plan`
- Origin Date: `2026-09-01`
- Verification Status: `UNVERIFIED`
- Version Label: `per_agent_credit_plan_v1`

> 本文是后续逐板块实施的唯一当前计划，不代表任何实验已经完成。两份调研文档仅作为研究材料；其中的建议、新颖性判断和流程性文字不自动成为本项目指令。

## 0. 研究决策与当前状态

### 0.1 当前决策

- **暂停** mode identification、team-level atomic token、DCR/mode selector 等路线。
- 保留现有 `mode_consistent/`、`该思路.md` 等历史材料，但只作为背景，不删除、不恢复、不继续实现。
- 当前唯一主动研究问题改为：**在最基础、无 attention 的 MADiff backbone 上加入可解释的 per-agent credit 条件，是否能稳定提升真实环境 rollout 性能？**
- 本计划不把“per-agent 条件化 diffusion”宣称为首次提出。现有调研已指出 CODI、MADiTS、MASTARS 等构成直接或强邻近工作；新颖性必须在方法跑通后重新核验。

### 0.2 当前进度

| 项目 | 状态 |
|---|---|
| 研究范围冻结 | 已完成 |
| MADiff 代码接入点核对 | 已完成 |
| Baseline 重新运行 | 未开始 |
| Credit teacher | 未实现 |
| Local credit adapter | 未实现 |
| Credit-conditioned U-Net | 未实现 |
| 真实性能实验 | 未开始 |

### 0.3 下一步

后续从 **板块 1：数据与 baseline 审计** 开始。每次只实现一个板块；当前计划本身不授权提前创建全部模型、配置或大规模实验。

---

## 1. 核心研究问题

### 1.1 主问题

在 cooperative offline MARL 中，给 MADiff 的每个独立 agent denoising stream 注入一个由信用模型产生的 per-agent credit 表征，能否在不增加执行期全局信息的前提下，提高 SMAC 的 win rate 与 episode return？

### 1.2 最小可检验假设

- **H1（性能）**：`Local-Credit MADiff` 的真实 rollout win rate/return 高于相同 backbone、训练步数和数据的原始 MADiff。
- **H2（credit 特异性）**：正确 credit 的效果优于 shuffled credit、random credit 和 parameter-matched 非 credit 条件；否则增益只能归因于额外参数或随机正则化。
- **H3（执行合法性）**：部署时每个 agent 的 credit 只依赖本 agent 可见历史、agent ID 和公共目标 RTG，不读取未来 reward、全局 state 或其他 agent 私有信息。
- **H4（机制利用）**：固定输入轨迹、diffusion noise 和 RTG 后，改变 agent \(i\) 的 credit 会以可重复方式改变该 agent 的预测，而不是被 denoiser 忽略。

### 1.3 本轮不检验的内容

- 不学习离散 coordination mode、atomic skill 或 team token。
- 不引入 cross-agent attention、agent ordering、sequential inpainting 或新的 CFG 公式。
- 不同时修改 inverse dynamics、noise scheduler、trajectory window 规则和 evaluator。
- 不声称估计到“真实因果贡献”；第一阶段只研究**具有明确构造方式且可校准的 credit proxy**。
- 不直接扩展到所有 OG-MARL 场景；先用 SMAC `3m-Good` 做 fail-fast pilot。

---

## 2. 代码事实与约束

以下结论来自当前工作区代码，而不是调研文档中的假设。

### 2.1 数据事实

- [`SequenceDataset`](./diffuser/datasets/sequence.py) 从 episode 中采样固定长度 trajectory windows，并返回 `x`、`cond`、`loss_masks`、`attention_masks`、可选 `returns` 和 `env_ts`。
- 当前 SMAC loader 把环境的单个 team reward 复制到所有 agent；因此数据中的 `rewards[..., i]` 在 SMAC 上是共享奖励副本，**不是 agent \(i\) 的 credit**。
- 当前 `returns` 由共享 reward 计算并形成 per-agent 形状，但语义仍是 team RTG。不能仅把该 tensor 改名为 `credit`。
- 当前 loader 没有向 diffusion batch 暴露 SMAC global state；credit teacher 若需要 centralized input，首版应使用 joint observation/history，或在板块 1 明确增加 state 数据链路，不能暗中假设 state 已存在。

### 2.2 模型事实

- [`SharedIndependentTemporalUnet`](./diffuser/models/ma_temporal_wrappers.py) 将输入从 \([B,T,N,F]\) 展平为 \([B\!\times\!N,T,F]\)，所有 agent 共享同一个 `TemporalUnet`，但不直接交换信息。
- 当前 team RTG 已按 agent stream 对齐后进入 `TemporalUnet` 的 embedding 路径。这是最小 credit 条件注入 seam。
- 首版 credit 只增加 `credit_mlp` 和条件 embedding；不改 U-Net 的 down/up blocks，不加 attention。

### 2.3 训练事实

- [`GaussianDiffusion.p_losses`](./diffuser/models/diffusion.py) 对 trajectory 加噪，模型预测 noise（或 \(x_0\)），再应用 masked diffusion loss。
- 当 `use_inv_dyn=True` 时，当前总损失为：

\[
\mathcal L_{\text{MADiff}}
=\frac{1}{2}\left(\mathcal L_{\text{diff}}+\mathcal L_{\text{inv}}\right).
\]

- 首个因果实验中保持上述目标不变。Credit teacher 和 local adapter 先单独训练并冻结，避免把“多了辅助 loss”和“credit 条件有效”混为一谈。

### 2.4 执行事实

- decentralized evaluator 为每个 agent 构造只保留自身历史的 planning condition，然后从各 planning result 中抽取对应 agent 的动作。
- 新 credit 路径必须遵守同一边界：agent \(i\) 的执行期 credit 只能由 \(h_t^i\)、agent ID 和公共 \(R_t^\star\) 计算。
- 任何使用 joint history、joint action、未来 reward 或 teacher critic 输出的 rollout 只能标记为 `PRIVILEGED/ORACLE`，不能作为 decentralized performance 结果。

---

## 3. 符号、credit 语义与信息权利

### 3.1 符号表

| 符号 | 含义 |
|---|---|
| \(N\) | agent 数量 |
| \(t\) | 环境时间步 |
| \(H\) | MADiff 预测 horizon |
| \(h_t^i\) | agent \(i\) 在时间 \(t\) 可见的局部历史 |
| \(\mathbf h_t\) | 所有 agent 局部历史组成的 joint history |
| \(a_t^i\) | agent \(i\) 的动作 |
| \(\mathbf a_t\) | joint action \((a_t^1,\ldots,a_t^N)\) |
| \(r_t\) | 环境提供的 team reward |
| \(R_t^\star\) | rollout 时给 MADiff 的公共目标 return-to-go |
| \(q_\psi^i(h_t^i,a_t^i)\) | credit teacher 中 agent \(i\) 的 local utility |
| \(Q_\psi^{\mathrm{tot}}(\mathbf h_t,\mathbf a_t)\) | credit teacher 对 joint behavior value 的估计 |
| \(I_t^i\) | agent \(i\) 的 importance/sensitivity proxy |
| \(A_t^i\) | agent \(i\) 当前动作相对 behavior baseline 的 quality/advantage proxy |
| \(z_t^i=[I_t^i,A_t^i]\) | 首版两维 per-agent credit target |
| \(\hat z_t^i\) | local credit adapter 在执行期预测的 credit |
| \(x_0^i\) | agent \(i\) 的干净 trajectory window |
| \(x_k^i\) | diffusion step \(k\) 的带噪 trajectory window |
| \(\epsilon\) | 添加到 \(x_0\) 的高斯噪声 |
| \(\epsilon_\theta\) | MADiff denoiser 预测的噪声 |

### 3.2 为什么不用单一“贡献度”直接控制噪声

单个高数值可能表示“该 agent 很重要”，也可能表示“该 agent 当前动作很好”。这两种含义对应不同控制策略。因此首版使用有固定语义的二维 target：

1. \(I_t^i\)：改变 agent \(i\) 的 utility 时 team value 有多敏感；
2. \(A_t^i\)：数据中 agent \(i\) 当前动作相对其 behavior-supported 替代动作是好还是坏。

这不是 latent mode；每个维度都有监督构造、量纲和独立诊断。

### 3.3 信息权利表

| 阶段/对象 | joint history | joint action | 当前/未来 team reward | global state | local history | 公共目标 RTG |
|---|---:|---:|---:|---:|---:|---:|
| Centralized credit teacher 训练 | 允许 | 允许 | 当前与离线后继允许 | 若数据存在则允许 | 允许 | 可选 |
| Teacher label 离线生成 | 允许 | 允许 | 仅离线样本允许 | 若数据存在则允许 | 允许 | 可选 |
| Local credit adapter 训练 | 只可作为 label 来源 | 不作为输入 | 不作为输入 | 不作为输入 | 允许 | 允许 |
| Diffusion 训练输入 | 禁止直接输入 teacher 私有量 | 禁止额外输入 | 数据 loss 正常使用，但不进入 credit adapter | 禁止新增 | 允许 | 允许 |
| Decentralized rollout | 禁止 | 禁止 | 只允许已发生 reward 用于更新公共 RTG | 禁止 | 允许 | 允许 |

硬规则：只要执行期 credit 由 centralized teacher 直接产生，该实验就不是 DE 结果。

---

## 4. 最小方法：Local-Credit MADiff

### 4.1 总体流程

```text
offline joint trajectories
        |
        v
centralized behavior-value teacher -- produces --> z_t^i = [importance, quality]
        |                                              |
        | offline labels only                          v
        +------------------------------------> local credit adapter
                                                       |
                                      h_t^i, agent_id, R_t* -> z_hat_t^i
                                                       |
                                                       v
                              SharedIndependentTemporalUnet credit embedding
                                                       |
                                                       v
                                      unchanged MADiff diffusion + inv loss
```

### 4.2 模块 A：Centralized credit teacher

#### 目标

使用一个既有 value-decomposition 思路的 teacher，对离线数据中的 joint behavior value 建模。Teacher 是训练期工具，不进入最终 decentralized policy。

#### 首版训练目标

首版使用 behavior-evaluation 式 TD target，避免对离线 joint action 做无约束 `max`：

\[
y_t=r_t+\gamma(1-d_t)
Q_{\bar\psi}^{\mathrm{tot}}(\mathbf h_{t+1},\mathbf a_{t+1}^{\mathcal D}),
\]

\[
\mathcal L_Q(\psi)=
\mathbb E_{\mathcal D}
\left[
\left(Q_\psi^{\mathrm{tot}}(\mathbf h_t,\mathbf a_t)-y_t\right)^2
\right].
\]

其中：

- \(\mathcal D\) 是离线数据集；
- \(\gamma\) 是环境 discount；
- \(d_t\) 表示 episode 在 \(t\) 后是否终止；
- \(\bar\psi\) 是 target teacher 参数；
- \(\mathbf a_{t+1}^{\mathcal D}\) 是数据中真实记录的下一步 joint action，而不是 critic 在 OOD joint action 上取最大值。

Teacher 首选 QMIX-style local utilities + monotonic mixer，因为它提供现成的 per-agent utility seam；此处不宣称 QMIX gradient 是因果 credit。

#### Credit target

Importance 使用归一化 mixer sensitivity：

\[
I_t^i=
\frac{
\left|\partial Q_\psi^{\mathrm{tot}}/\partial q_\psi^i\right|
}{
\sum_{j=1}^{N}\left|\partial Q_\psi^{\mathrm{tot}}/\partial q_\psi^j\right|+\varepsilon_I
}.
\]

其中 \(\varepsilon_I>0\) 防止分母为零。\(I_t^i\) 只表示局部敏感性。

Action quality 使用 behavior-constrained counterfactual advantage：

\[
A_t^i=q_\psi^i(h_t^i,a_t^i)
-\mathbb E_{\tilde a^i\sim\hat\mu_i(\cdot\mid h_t^i)}
q_\psi^i(h_t^i,\tilde a^i).
\]

其中 \(\hat\mu_i\) 是由离线数据拟合的 behavior action distribution；SMAC 中只在 legal actions 上计算期望。最终 teacher label 为：

\[
z_t^i=[I_t^i,\operatorname{clip}(A_t^i/s_A,-c_A,c_A)].
\]

\(s_A\) 是仅由 training split 估计的 robust scale，\(c_A\) 是预先固定的截断阈值。该处理避免 critic 的极端值直接支配条件 embedding。

#### 重要限定

- 不要求 \(\sum_i A_t^i=r_t\)；counterfactual advantage 与 reward decomposition 不是同一对象。
- 不把 \(I_t^i\) 称为 causal contribution。
- Label 必须按 episode split 生成；validation/test episode 不参与 teacher 拟合。

### 4.3 模块 B：Local credit adapter

Teacher label 在执行期不可用，因此训练一个小型共享 adapter：

\[
\hat z_t^i=C_\phi(h_t^i,e_i,R_t^\star),
\]

其中 \(e_i\) 是 agent ID embedding。Adapter loss 为：

\[
\mathcal L_C(\phi)=
\lambda_I\,\operatorname{Huber}(\hat I_t^i,I_t^i)
+\lambda_A\,\operatorname{Huber}(\hat A_t^i,A_t^i).
\]

首版要求：

- 所有 agent 共享 adapter 参数；
- 模型只使用当前 MADiff 已允许的 local history、agent ID 和公共 RTG；
- adapter 单独训练、验证、冻结；
- diffusion 训练和 rollout 都输入 \(\hat z_t^i\)，而不是训练时用 teacher、测试时换 local predictor；
- 训练 diffusion 时对 \(\hat z_t^i\) `stop_gradient`，保持归因清晰。

这意味着首版的 credit 不增加执行期新信息，而是把 centralized teacher 的归纳偏置蒸馏成一个局部、低维、可诊断的辅助表征。若它不能从 local information 预测，就应停止该路线，而不是偷用 centralized teacher。

### 4.4 模块 C：Credit-conditioned SharedIndependentTemporalUnet

对每个 agent stream 构造 credit embedding：

\[
e_{c,t}^i=\operatorname{MLP}_c(\hat z_t^i).
\]

将它与现有 diffusion timestep embedding 和 return embedding 拼接：

\[
e_t^i=[e_k; e_R; e_{c,t}^i; e_{\mathrm{env}}].
\]

然后沿用原始 `TemporalUnet` 的 residual temporal blocks。Denoiser 写为：

\[
\hat\epsilon^i=epsilon_\theta
(x_k^i,k,R_t^\star,\hat z_t^i,h_t^i).
\]

首版只做 embedding concatenation，原因是它能复用现有 return-condition 路径，改动最少。FiLM、cross-attention、credit-CFG 均留到首轮正向结果后的消融阶段。

### 4.5 模块 D：保持 MADiff loss 不变

Credit-conditioned diffusion 的首版训练目标仍为：

\[
\mathcal L_{\mathrm{diff}}
=\mathbb E_{x_0,k,\epsilon}
\left[
\left\|
M\odot
\left(\epsilon-\epsilon_\theta(x_k,k,R^\star,\hat z)\right)
\right\|_2^2
\right],
\]

其中 \(M\) 是现有 `loss_masks`。若启用 inverse dynamics：

\[
\mathcal L_{\mathrm{total}}
=\frac{1}{2}
(\mathcal L_{\mathrm{diff}}+\mathcal L_{\mathrm{inv}}).
\]

不在首版把 \(\mathcal L_C\) 加入 diffusion optimizer。只有当冻结版验证有效后，才允许把 joint training 作为独立消融。

---

## 5. 实验变量与公平对照

### 5.1 独立变量

主独立变量是 denoiser 接收的 agent-level 条件：

| 编号 | 条件 | 用途 |
|---|---|---|
| B0 | 原始 team RTG，无 credit | 主 baseline |
| B1 | parameter-matched condition，输入 agent ID/随机常量 | 排除新增参数收益 |
| C1 | 正确 local credit \(\hat z_t^i\) | 主方法 |
| C2 | 跨 episode/agent shuffled credit | 检验 credit 配对语义 |
| C3 | 只用 \(I_t^i\) | importance 消融 |
| C4 | 只用 \(A_t^i\) | quality 消融 |
| O1 | teacher credit | 仅离线/privileged 上界诊断，不计 DE 主结果 |

首轮 pilot 只跑 B0、B1、C1、C2；C3/C4/O1 在 C1 通过性能门后运行。

### 5.2 控制变量

- 数据 split、trajectory windows、normalizer、history horizon、prediction horizon；
- `SharedIndependentTemporalUnet` backbone 和 U-Net 宽度；
- diffusion scheduler、noise steps、inverse model、optimizer、batch size、训练更新数；
- seed 集合、checkpoint 选择规则、目标 RTG、evaluator 和 episode 初始种子；
- 参数量差异：B1 增加与 C1 相同规模的 condition MLP；
- 计算量：记录 train time、sampling latency、peak GPU memory 和 checkpoint size。

### 5.3 主要因变量

1. SMAC win rate；
2. mean episode return；
3. 相对 B0 的 paired improvement；
4. credit adapter 的 held-out prediction quality；
5. credit intervention sensitivity；
6. 参数量、采样延迟和显存开销。

---

## 6. 分板块实施流程

### 板块 1：数据与 baseline 审计

#### 目标

确认 `3m-Good` 的真实字段、reward 语义、window 边界和当前 baseline 可复现性。此阶段不实现 credit。

#### 任务

1. 输出一个 episode 和一个 batch 的字段、dtype、shape、取值范围与 mask 比例。
2. 证明 SMAC rewards 是否逐 agent 完全相同；检查是否存在 global `state`、legal actions 和 agent ID。
3. 检查 train/validation/test 按 episode 切分，禁止同一 episode 的 windows 跨 split。
4. 固定 baseline 配置：`SharedIndependentTemporalUnet`、无 attention、`horizon=4`、`history_horizon=20`；新实验禁止从旧 checkpoint 隐式 resume。
5. 至少用 3 个训练 seed 跑 B0，记录训练曲线和真实 rollout 分布。

#### 产物

- `docs/per_agent_credit/data_audit.md`
- 独立 baseline config，不覆盖当前配置
- baseline metrics JSON/CSV 和运行命令

#### 验收门 G1

- batch schema 与 mask/window 边界测试通过；
- 数据 split 无 episode 泄漏；
- B0 能完成训练、checkpoint load 和真实 rollout；
- 三个 seed 没有系统性 NaN、全零动作或 evaluator 失败。

#### 停止条件

若 baseline 本身不能稳定运行，停止 credit 开发，先只修 baseline；不得用 credit 模块掩盖基础问题。

### 板块 2：Centralized credit teacher

#### 目标

在不修改 MADiff 的情况下，训练并验证 behavior-value teacher，生成 \([I,A]\) labels。

#### 任务

1. 实现最小 QMIX-style teacher、target network 和 behavior-policy head。
2. 使用 episode-level split 和 dataset next action TD target。
3. 对 SMAC legal actions 计算 behavior-constrained \(A_t^i\)。
4. 生成 label 时保存 teacher version、split、normalization statistics 和 label checksum。
5. 用至少 3 个 teacher seed 检查 credit rank/sign 稳定性。

#### 必测项

- held-out TD error 与 reward/return scale；
- behavior action log-likelihood；
- \(I\) 的归一化误差 \(|\sum_i I_i-1|\)；
- \(A\) 的分布、截断比例和 sign balance；
- seed 间 rank correlation；
- agent permutation 后输出是否按相同置换变化。

#### 验收门 G2

- teacher 优于 constant-value predictor；
- behavior head 优于 action-frequency-only baseline；
- credit 不坍缩成每个 agent 恒定值；
- credit 排序在 teacher seeds 间具有可接受稳定性；具体阈值在看到 candidate diffusion 结果前，用板块 2 validation split 预先登记。

#### 停止条件

若不同 teacher seed 给出互相矛盾的 credit 排序，或 credit 只编码 agent ID/episode time，则停止注入 diffusion，先报告“credit proxy 不可靠”。

### 板块 3：Local credit adapter

#### 目标

验证 centralized credit 是否能从 DE 可用信息中蒸馏出来。

#### 任务

1. 输入只使用 \(h_t^i\)、agent ID、\(R_t^\star\)。
2. 与以下基线比较：均值预测、agent-ID-only、agent-ID + environment timestep。
3. 报告 \(I/A\) 的 MAE、Spearman rank、quality sign accuracy 和 calibration plot。
4. 审计模型 forward signature，确认没有 joint tensor 或未来字段。

#### 验收门 G3

- local adapter 在 held-out episodes 上明确优于上述简单基线；
- 输出不坍缩；
- 执行期信息泄漏单元测试通过；
- train/test credit 分布偏移处于预先登记的可接受范围。

#### 停止条件

若 local adapter 无法预测 teacher credit，则不允许在 DE rollout 中直接调用 teacher。该结果说明 credit 没有可部署性，路线在此暂停。

### 板块 4：Credit condition 接入 U-Net

#### 目标

以最小改动把 \(\hat z_t^i\) 接入 `SharedIndependentTemporalUnet` 和 `GaussianDiffusion`。

#### 预计修改边界

- `diffuser/models/ma_temporal_wrappers.py`
- `diffuser/models/temporal.py`
- `diffuser/models/diffusion.py`
- `run_scripts/train.py` 与 `run_scripts/evaluate.py`
- `diffuser/datasets/sequence.py` 或新的 credit dataset wrapper
- 与上述改动一一对应的 tests/config

实际实施前必须重新检查工作区 diff；若现有修改与这些文件重叠，先区分用户修改与本研究修改。

#### 必须先写的测试

1. credit shape：\([B,2,N]\rightarrow[B\times N,2]\) 与 agent 顺序严格一致；
2. agent permutation equivariance；
3. `credit_condition=False` 时与 B0 forward 行为兼容；
4. 固定 noise 下改变 agent \(i\) credit，输出变化可测；
5. shuffled credit 的配对逻辑正确；
6. DE evaluator 不能访问 teacher-only tensor；
7. loss、backward、EMA、save/load smoke test。

#### 验收门 G4

- 全部新增测试与原有测试通过；
- `credit_condition=False` 不改变 baseline tensor contract；
- 新增参数量、显存和延迟被记录；
- 无 NaN/Inf，credit gradient 路径符合冻结设计。

### 板块 5：最小 pilot

#### 目标

只回答一个问题：C1 是否比 B0 更好，并且这种差异是否来自正确 credit。

#### 环境与配置

- 环境：SMAC `3m-Good`；
- Backbone：`SharedIndependentTemporalUnet`；
- 实验臂：B0、B1、C1、C2；
- 每臂相同的 5 个训练 seed；
- 每个 checkpoint 使用相同 evaluation seeds 和相同目标 RTG；
- checkpoint 选择规则必须在查看 C1 结果前固定，不能按测试 win rate 挑最好一步。

#### 验收门 G5：Pilot go/no-go 判据

先用 B0 三 seed 估计 baseline 波动，再在不查看 C1 的情况下登记最小有意义提升 \(\delta_{\min}\)。默认下限为 win rate 绝对提升 3 percentage points；若 baseline seed 波动更大，则提高而不是降低阈值。

进入下一阶段需同时满足：

1. C1 相对 B0 的 mean win-rate improvement \(\ge\delta_{\min}\)；
2. 至少 4/5 个 paired seeds 不劣于 B0；
3. C1 明确优于 C2，且不是只优于 B0；
4. B1 不能解释 C1 的全部增益；
5. sampling latency 增幅不超过 10%，或给出明确收益—成本解释；
6. 无 legality、termination 或 information leakage 回归。

不满足时先判定 `NO-GO` 或 `REVISE`，不得直接扩大实验矩阵。

### 板块 6：机制消融

仅在 G5 通过后执行：

1. C3 importance-only 与 C4 quality-only；
2. teacher-credit privileged upper bound；
3. credit dropout/noise robustness；
4. one-agent credit intervention；
5. adapter frozen vs. joint training；
6. embedding concatenation vs. FiLM。FiLM 不是默认主方法，只有此时才实现。

机制成立至少需要：正确 credit 优于 shuffled credit；双通道优于或能解释单通道；credit intervention 与输出变化具有稳定对应关系。

### 板块 7：扩展与最终确认

仅在机制消融支持 H2/H4 后扩展：

1. `3m-Medium`、`3m-Poor`，检验异构质量；
2. 一个更强协调场景，如 `2s3z` 或 `5m6m`；
3. 至少一个 role/agent 数不同的场景；
4. 最终关键比较提高到 10 个训练 seeds，报告每个 seed；
5. 根据可运行性再选择 MADiTS/CODI/DoF 等外部 baseline，不在 pilot 前承担全部复现成本。

#### 最终结论规则

- 只有真实 rollout 的跨 seed 改善才能支持“性能提升”。
- Offline denoising loss 下降、credit prediction 准确或静态 trajectory 指标改善都不能替代 rollout。
- 只有 local-credit 结果才能支持 decentralized execution；teacher-credit 只作为 upper bound。
- 若只在一个数据 split/一个 seed 有效，结论必须标记为 exploratory。

### 板块 8：研究决策与文档化

最终将结果归入以下之一：

| 结论 | 判据 | 后续动作 |
|---|---|---|
| GO | 性能、credit 特异性、DE 合法性均成立 | 扩基准并重新做新颖性检索 |
| MECHANISM-ONLY | credit 被利用，但 rollout 无稳定提升 | 作为负结果/诊断，不包装成性能方法 |
| PRIVILEGED-ONLY | teacher credit 有效，local adapter 无效 | 研究 local distillation，不宣称 DE |
| NO-GO | oracle/local 均无作用或 shuffled 同样有效 | 停止路线，保留完整负结果 |
| INVALID | baseline、数据或 evaluator 存在问题 | 修复基础设施后重新登记实验 |

---

## 7. 统计分析与结果记录

### 7.1 分析单位

- 主要分析单位是**独立训练 seed**，不是同一模型下的 evaluation episode。
- Evaluation episodes 用于估计每个 seed 的 rollout 不确定性，不能伪装成独立训练重复。
- 使用相同训练 seed、相同 evaluation seed 和相同 initial conditions 做 paired comparison。

### 7.2 必报内容

- 每个 seed 的 win rate、return、训练步数和 checkpoint；
- mean、standard deviation、median、95% confidence interval；
- C1-B0 和 C1-C2 的 paired difference；
- effect size 与原始 seed 表；
- 失败 run、NaN、重试和被排除 run，不能只汇报成功结果；
- 参数量、checkpoint size、训练时长、sampling latency、peak memory。

### 7.3 多重比较

Pilot 的预登记主比较只有 `C1 vs B0` 和 `C1 vs C2`。其余比较视为 secondary；进入完整矩阵后再使用 Holm correction 或明确标为 exploratory。

---

## 8. 配置与产物约定

后续实现建议使用独立 namespace，避免与暂停的 mode 路线混合：

```text
diffuser/models/credit/                 # teacher 与 local adapter
exp_specs/per_agent_credit/smac/3m/     # 本研究配置
tests/per_agent_credit/                 # shape、leakage、loss、save/load 测试
docs/per_agent_credit/                  # 数据审计、实验登记与结果
```

这只是目录边界建议；板块 1 审计完成前不创建空架构。

每个 experiment run 至少保存：

- 完整 resolved config；
- git commit/diff 摘要；
- dataset fingerprint 与 split seed；
- teacher/adapter/diffusion checkpoint 的独立版本；
- optimizer、EMA、RNG 和训练 step；
- evaluation raw episode records；
- 运行命令、环境版本与异常日志。

---

## 9. 风险登记

| 风险 | 最小诊断 | 决策 |
|---|---|---|
| 共享 reward 被误称为 credit | 检查 agent reward equality | 相同则只作 team reward，不作 credit label |
| QMIX sensitivity 不稳定 | 多 teacher seed rank test | 不稳定则停止注入 |
| Credit 只编码 agent ID/时间 | 与 ID-only/time-only 比较 | 无额外预测力则 NO-GO |
| Local adapter 无法蒸馏 | held-out adapter metrics | 禁止 teacher 直接用于 DE |
| Critic OOD 外推 | dataset next-action TD、legal behavior baseline | 不使用无约束 joint-action max |
| Denoiser 忽略 credit | 固定 noise intervention | 无响应则检查注入或停止 |
| 增益来自额外参数 | B1 parameter-matched control | B1 同样提升则不归因 credit |
| 增益来自任意随机标签 | C2 shuffled control | C2 同样提升则不归因 credit |
| Train/test condition shift | 训练与 rollout 均使用 frozen local output | shift 过大则停止 |
| DE 信息泄漏 | forward signature + runtime assertion | 任一泄漏使结果 INVALID |
| 单 seed 偶然性 | paired multi-seed | 不扩展、不作结论 |
| 新颖性被近邻工作覆盖 | 正向 pilot 后更新检索 | 调整 claim，不倒推实验结论 |

---

## 10. 成功标准

本研究不是以“代码能跑”作为完成，而要依次满足：

1. **定义成立**：team reward、teacher proxy、local credit 三者语义不混淆；
2. **估计成立**：teacher 和 adapter 均通过 held-out、稳定性及简单基线检查；
3. **执行成立**：rollout credit 完全符合 decentralized information boundary；
4. **机制成立**：正确 credit 优于 shuffled/parameter-matched controls；
5. **性能成立**：真实 rollout 在 paired multi-seed 上达到预登记的最小有效提升；
6. **成本可接受**：额外参数和推理开销有记录且与收益相称；
7. **结论克制**：不把 sensitivity 当因果贡献，不把 privileged result 当 DE，不把单场景 pilot 当普遍结论。

任一前置标准失败，就停在对应板块记录负结果，不继续堆叠 attention、CFG、ordering 或新的 latent representation。

---

## 11. 研究依据与边界

- [`offline_marl_research_direction.md`](./offline_marl_research_direction.md)：提供方向候选和初步方法构想，其中“扩散与 credit 完全空白”等宽泛判断不直接采纳。
- [`per_agent_credit_diffusion_offline_marl_literature_scan.md`](./per_agent_credit_diffusion_offline_marl_literature_scan.md)：提供 CODI、MADiTS、MASTARS、DoF、MACCA、SIT 等邻近工作与 credit 语义风险。
- [`MADiff.pdf`](./MADiff.pdf)：后续实现前用于核对原始 MADiff 训练、采样和 DE 论述。
- 当前仓库代码：用于确认实际 tensor、loss 和 evaluator 路径；后续每个板块仍需以实施时的 live code 为准。

检索边界会变化。只有 pilot 结果值得继续后，才投入完整的新颖性复核与论文 claim 设计。
