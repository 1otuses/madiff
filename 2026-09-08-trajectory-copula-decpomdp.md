**轨迹 Copula 与 Dec-POMDP：表示、学习和执行的理论边界**

日期：2026-09-08。回应用户关于 DM 边缘＋Copula、动作到轨迹推广、MMDP 到 Dec-POMDP 推广及两阶段训练收敛的问题。以下是原文核查和数学推理，没有实施算法或运行实验，也不作新颖性声明。

**结论**

轨迹作为向量边缘有 Vector Sklar 理论可参考；冻结边缘后学习依赖也是合理的建模分工。但 Sklar 的存在性不保证密度存在、所选参数族充分、优化收敛或分散可执行。必须先定义可执行的条件耦合类，再决定如何估计它。统计依赖还可能来自环境、行为数据混合等，不必然代表高回报协作。

**1. 唯一性不是收敛性**

经典 Sklar 给出联合 CDF 与标量边缘 CDF 的 copula 表示；连续边缘下 copula 唯一，非连续情况下通常只在边缘值域的乘积上唯一。要使用密度形式，还需要相应绝对连续性。例：U 均匀分布，(U,U) 的两个边缘连续，copula C(u,v)=min(u,v) 唯一，但联合概率集中在对角线，没有二维 Lebesgue 密度。[Segers 本人讲义，Sklar 与 comonotonic copula](https://www.columbia.edu/~rf2283/Conference/1Fundamentals%20(1)Seagers.pdf)

因此不能以 Sklar 为“唯一 copula 密度”或“SGD 保证收敛”的一般依据。SMAC 的离散动作、包含离散特征或精确动力学约束的观察轨迹，尤其需要明确采用 CDF、概率测度还是密度模型。[Copulas 原论文 §3.1](https://arxiv.org/pdf/2107.04750)

合法性可以由参数化保证。例如正定、单位对角的相关矩阵定义 Gaussian copula。但 Gaussian copula 的相关矩阵 MLE 一般也不等价于无约束 Gaussian 协方差估计；相关矩阵约束不能凭“精度矩阵方向的凹性”直接获得全局收敛。神经网络预测条件参数后通常还引入非凸性。[Zwiernik–Uhler–Richards，§1、§4](https://arxiv.org/pdf/1408.5604)

统计一致性是另一问题：通常需要正确/足够的模型族、可识别性、适当数据条件及对总体目标的一致估计与优化。冻结有误差的边缘不会消除误差；优化损失下降也不证明真实联合分布已恢复。

一个可证明但受限的替代是固定 M 个合法 copula 密度 c_m，只学习混合权重：

$$
c_w(u)=\sum_{m=1}^M w_m c_m(u),\qquad
w_m\ge0,\quad\sum_mw_m=1.
$$

混合仍具有均匀边缘。对固定伪样本 u^(b)，经验负对数似然

$$
L(w)=-\sum_b\log\left(\sum_mw_mc_m(u^{(b)})\right)
$$

是凸函数：每项是负对数作用于非负仿射函数；在样本混合密度为正的区域，Hessian 是半正定的外积之和。因此可以对这一步使用具有相应收敛保证的凸优化方法。若包含严格正密度成分并控制可行域，可避免样本处对数未定义。这不保证全局最优解唯一，也不保证有限基函数拟合任意真实 copula。若同时学习 c_m 或用神经网络输出权重，不能继续沿用这一凸性结论。该段是数学推导，不是所引 MARL 论文的贡献。

**2. Action 到 trajectory：每个边缘变成一个向量块**

令 X_i=(o_{i,k+1},...,o_{i,k+H}) 为 agent i 当前生成的未来观察计划，h_{i,k} 为局部观察动作历史。已知初始观察不必重复作为随机坐标。X_i 的联合分布本身需要表达时间与特征之间的依赖。

不能直接把多维联合 CDF 的自身评价 F_i(X_i) 当作一个 Uniform(0,1) 随机数。反例：独立均匀 U,V 的联合 CDF 为 F(u,v)=uv，而 F(U,V)=UV 并非均匀。把所有时间/特征坐标拆成标量后可用经典 Sklar，但这样 copula 也承担 agent 内部的时间依赖，不再是“每个 DM 保留整段轨迹边缘”的原设计。

Fan–Henry 的 Vector Copulas 利用向量分位数/运输，把多维边缘变成均匀向量块，再表达块间依赖。Theorem 1 提供存在性；其唯一性结论附有向量边缘绝对连续及支撑条件，并使用所定义的规范向量分位数。[Vector Copulas，§2.2–2.3，Theorem 1](https://arxiv.org/html/2009.06558v2)

普通 diffusion 能采样，并不意味着能计算这一向量 rank/quantile，也不意味着存在便宜可用的 CDF 和逆 CDF。确定性 DDIM 可以视为固定的生成映射 G_i，但不能未经证明将它等同于 Vector Sklar 的规范运输。若采用它，应证明具体映射下的边缘保持与联合推前性质。

**3. Dec-POMDP 的条件不能直接替换**

先为标量连续 X_i 写清最小问题，假设密度存在，记 H=(h_1,...,h_N)。条件 Sklar 对每个固定 H 使用的是真实联合条件边缘：

$$
p^\star(\mathbf X\mid H)
=c_H\big(F_1^\star(X_1\mid H),...,F_N^\star(X_N\mid H)\big)
\prod_i p_i^\star(X_i\mid H).
$$

用户方案冻结的是 p_i(X_i|h_i)，两者一般不同。如果构造

$$
q(\mathbf X\mid H)
=c_H\big(F_1(X_1\mid h_1),...,F_N(X_N\mid h_N)\big)
\prod_i p_i(X_i\mid h_i),
$$

且 c_H 是合法 copula 密度，对除 X_i 外的坐标积分、作 u_j=F_j(X_j|h_j) 的变量代换，得到

$$
q(X_i\mid H)=p_i(X_i\mid h_i)
\int c_H(\mathbf u)\,d\mathbf u_{-i}
=p_i(X_i\mid h_i).
$$

所以精确恢复 p* 的必要条件是 p_i*(X_i|H)=p_i(X_i|h_i)。Copula 保持边缘，不能修复这个条件不匹配。即便后续密度比网络学得足够强，如果它通过改变条件边缘来弥补误差，它也已不再是保持这些边缘的 proper copula。

直观反例：agent 1 看到均匀私有位 B，agent 2 无法看到；目标为两者都选择 B。agent 2 给定自身历史的边缘为左右各半，给定联合历史的边缘则是确定的 B。共享随机数不传递 B，冻结该局部边缘后无法复现上述集中决策。这个问题是信息可获得性，不是 copula 容量不足。

原 Copulas 论文实现使用全局状态；脚注提出可将各边缘网络改为部分状态输入，但算法 2 仍是联合 copula 采样。这一修改本身没有建立本项目所需的分散执行保证。[Copulas 原文 §3.2、脚注 2、Algorithm 2](https://arxiv.org/pdf/2107.04750)

**4. 同时满足局部条件与无通信执行的模型类**

带共同可得信息 C 和公共随机源 Z 的一种可执行形式为

$$
p_{\rm dec}(\mathbf X\mid H,C)
=\int p(z\mid C)\prod_iq_i(X_i\mid h_i,C,z)\,dz.
$$

这里考虑当次规划的新随机源，给定共同信息 C 后独立于已有私有历史。每个 agent 使用同一个 z、自己的 h_i，计算自己的输出。公共随机源可以通过预先约定随机流和索引实现，不要求在线交换观察。若使用有记忆的相关装置，需把相关装置状态及其与历史的依赖明确纳入模型，而不能仍无条件假设当次种子与历史独立。

Dec-POMDP 中 correlation device 有正式先例，原文明确其随机数可在执行前约定，装置不提供其他 agent 的观察信息。它不等价于带全局状态的协调器。[Policy Iteration for Decentralized Control of Markov Decision Processes，相关装置部分](https://arxiv.org/pdf/1401.3460)

任意全局条件 copula c_H 未必能表示为上述局部模型。甚至条件边缘已经只依赖 h_i，也仍不是充分条件：令两人分别看到私有位 u,v，要求各自输出均匀且 X_1 XOR X_2=u v 对所有输入组合恒成立。固定公共/私有随机实现后，局部输出函数的四个异或等式相加产生 0=1，故不存在完美的局部实现。该例仅用作必要条件不充分的数学说明，并非实际 benchmark 设定。

**5. DM 版本的执行顺序与边缘保持**

原标量 copula 流程是先联合采样 u~C，再各自计算 F_i^{-1}(u_i)。各 agent 用独立随机数分别采样一个完整 C，再只拿自己的坐标，会丢失原本的跨 agent 依赖。执行时先独立生成最终动作/轨迹，再乘上 copula 密度，也不是一个已定义的有效采样算法。

对于局部 DM，可采用基础噪声上的一个受限耦合模型：

$$
z_k\sim\mathcal N(0,I_r),\quad
\eta_{i,k}\sim\mathcal N(0,I_{D_i}),\quad
\xi_{i,k}=A_\phi(h_{i,k},e_i)z_k+B_\phi(h_{i,k},e_i)\eta_{i,k},
$$
$$
A_iA_i^\top+B_iB_i^\top=I_{D_i}.
$$

z_k 公共，eta_i 私有且相互独立，e_i 为可选身份信息。给定当前历史，每个 xi_i 都保持 N(0,I)，联合噪声的交叉协方差为 A_i A_j^T。固定 r、轨迹维度与局部模块成本时，各 agent 无须构造 N 个输出。完整协方差或所有 agent 的输入网络则不自动具有这一规模优势。

固定局部生成器及确定性采样规则后，

$$
\hat X_i=G_{\theta_i}(\xi_{i,k}\mid h_{i,k}),\qquad
a_{i,k}=g_{\psi_i}(o_{i,k},\hat o_{i,k+1}).
$$

由于输入边缘保持，推前分布 G_i#N(0,I) 也保持；改变的是生成计划间的耦合。该结论来自具体构造，而非直接从 Sklar 推出。若需要多步随机去噪，应把完整随机流纳入分析，或先使用确定性 sampler。该局部线性 Gaussian 因子模型是受限族，不代表一般条件 copula。

第一阶段学习局部轨迹边缘、第二阶段冻结后学上述依赖，逻辑上成立。但第二阶段还要选择可计算的目标：普通 DM 不直接提供可用逆运输，不能照抄基于 F_i 和逆 CDF 的 MLE。可研究额外的可逆运输，或利用生成样本做分布匹配/数据支持内的兼容性学习；这些选择各有额外假设，不能宣称已实现一致的 copula 最大似然。

**6. 计划相关性与真实协同效果**

生成的未来观察段是计划变量。真实状态由 P(s_{k+1}|s_k,联合动作) 演化；逆动力学误差、碰撞、部分观察等可能使计划不可实现。联合计划分布拟合正确也不是实际轨迹分布正确或高回报的证明。边缘保持是在固定历史下的规划分布性质，联合策略改变后的闭环历史分布可能变化。

建议当前理论目标限定为三个可核对命题：指定条件与运输下的表示；给定耦合结构的局部边缘保持与分散可实现性；某个明确估计器或受限优化问题的收敛性质。整个神经算法收敛到真实联合轨迹最优分布，暂时没有依据。
